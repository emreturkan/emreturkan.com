// Guestbook actions will initialize Supabase inside each action once the
// feature is enabled. Keeping module evaluation side-effect free allows the
// rest of the site to build when guestbook credentials are not configured.
